{
  "name": "ojisan-antipatterns",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "おじさんアンチパターン集 — A Pokedex-style guide to middle-aged traits we want to avoid",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate": "node scripts/generate-entry.js",
    "generate:initial": "node scripts/generate-initial.js"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}